#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
  for (char letter = 'a'; lettter < 27; letter++);
    _putchar(letter);
    
  _putchar('\n');
}
