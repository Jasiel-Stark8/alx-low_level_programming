#include "main.h"
/**
 * *_memcpy - cpoies byte from memory area src to dest
 * @dest: destination where memory is copied to
 * @src: source where memory is copied from
 *
 * Returns: destination memory
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
  int i;
  int j;
  for (i = 0; j > i; i++)
    {
      dest[i] = src[j]
	n--;
    }
  return (dest);
}
